{
  "name": "pkg",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "preversion": "npm-git-library-version preversion",
    "version": "npm-git-library-version version dist",
    "build": "(mkdir dist || exit 0) && echo a > dist/a.txt && echo b > dist/b.txt && echo cruft > cruft.txt",
    "test": "echo \"Pass testing\" && exit 0"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "npm-git-library-version": "file:..\\..\\.."
  }
}
